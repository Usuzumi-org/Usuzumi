export const browserExpressionFormsNavigationInteractions = `const searchClearInitiallyHidden = searchClear.hidden;
const searchClearHasSvg = Boolean(searchClear.querySelector('svg'));
const searchClearText = searchClear.textContent.trim();
const searchClearStyle = getComputedStyle(searchClear);
const searchClearWidth = searchClearStyle.width;
const searchClearHeight = searchClearStyle.height;
const searchClearBorderWidth = searchClearStyle.borderTopWidth;
const searchClearBorderRadius = searchClearStyle.borderTopLeftRadius;
const searchClearBackground = searchClearStyle.backgroundColor;
const searchClearBoxShadow = searchClearStyle.boxShadow;
const searchClearBackdropFilter = searchClearStyle.backdropFilter || searchClearStyle.webkitBackdropFilter || '';
const formInvalidBefore = consumerRequiredField.classList.contains('is-invalid') && consumerRequiredInput.getAttribute('aria-invalid') === 'true';
const formValidBeforeManualValidate = window.Usuzumi.validateForm(consumerForm);
const formInvalidAfterManualValidate = consumerRequiredField.classList.contains('is-invalid') && consumerRequiredInput.getAttribute('aria-invalid') === 'true';
consumerRequiredInput.value = 'Usuzumi';
consumerRequiredInput.dispatchEvent(new Event('input', { bubbles: true }));
const formInvalidAfter = consumerRequiredField.classList.contains('is-invalid');
const formValidAfter = window.Usuzumi.validateForm(consumerForm);
click(searchClear);
const searchValueAfterClear = searchInput.value;
const searchClearHiddenAfterClear = searchClear.hidden;
click(passwordToggle);
const passwordTypeAfterToggle = passwordInput.type;
const passwordPressedAfterToggle = passwordToggle.getAttribute('aria-pressed');
const passwordToggleHasSvg = Boolean(passwordToggle.querySelector('svg'));
const passwordToggleText = passwordToggle.textContent.trim();
const passwordToggleStyle = getComputedStyle(passwordToggle);
const passwordToggleWidth = passwordToggleStyle.width;
const passwordToggleHeight = passwordToggleStyle.height;
const passwordToggleBorderWidth = passwordToggleStyle.borderTopWidth;
const passwordToggleBorderRadius = passwordToggleStyle.borderTopLeftRadius;
const passwordToggleBackground = passwordToggleStyle.backgroundColor;
const passwordToggleBoxShadow = passwordToggleStyle.boxShadow;
const passwordToggleBackdropFilter = passwordToggleStyle.backdropFilter || passwordToggleStyle.webkitBackdropFilter || '';
const stepperBorderColor = getComputedStyle(stepper).borderTopColor;
focusVisible(stepperIncrement);
await wait(0);
const stepperButtonFocusedActive = document.activeElement === stepperIncrement;
const stepperButtonFocusedBorderColor = getComputedStyle(stepper).borderTopColor;
const stepperButtonFocusedBoxShadow = getComputedStyle(stepper).boxShadow;
const stepperButtonFocusedTargetStyle = getComputedStyle(stepperIncrement);
const stepperButtonFocusedTargetBoxShadow = stepperButtonFocusedTargetStyle.boxShadow;
const stepperButtonFocusedOutlineStyle = stepperButtonFocusedTargetStyle.outlineStyle;
const stepperButtonFocusedOutlineWidth = stepperButtonFocusedTargetStyle.outlineWidth;
const stepperButtonFocusedOutlineColor = stepperButtonFocusedTargetStyle.outlineColor;
const stepperButtonFocusedOutlineOffset = stepperButtonFocusedTargetStyle.outlineOffset;
stepperIncrement.blur();
await wait(0);
click(stepperIncrement);
const stepperValueAfterIncrement = stepperInput.value;
const stepperIncrementDisabledAfterMax = stepperIncrement.disabled;
const stepperInputAppearance = getComputedStyle(stepperInput).appearance || getComputedStyle(stepperInput).webkitAppearance || '';
const consumerSlider = document.querySelector('#consumer-slider');
const sliderValueBefore = consumerSlider.style.getPropertyValue('--uzu-slider-value').trim();
consumerSlider.value = '75';
consumerSlider.dispatchEvent(new Event('input', { bubbles: true }));
const sliderValueAfter = consumerSlider.style.getPropertyValue('--uzu-slider-value').trim();
const sliderStyle = getComputedStyle(consumerSlider);
const sliderHeight = sliderStyle.height;
const sliderTrackHeightToken = sliderStyle.getPropertyValue('--uzu-slider-track-height').trim();
const sliderThumbSizeToken = sliderStyle.getPropertyValue('--uzu-slider-thumb-size').trim();
const sliderTrackToken = sliderStyle.getPropertyValue('--uzu-slider-track').trim();
const sliderTrackBorderToken = sliderStyle.getPropertyValue('--uzu-slider-track-border').trim();
const sliderFillToken = sliderStyle.getPropertyValue('--uzu-slider-fill').trim();
const sliderThumbToken = sliderStyle.getPropertyValue('--uzu-slider-thumb').trim();
const sliderThumbBorderToken = sliderStyle.getPropertyValue('--uzu-slider-thumb-border').trim();
const sliderFillColor = readResolvedColorValue(sliderFillToken, consumerSlider);
const sliderThumbColor = readResolvedColorValue(sliderThumbToken, consumerSlider);
const sliderThumbBorderColor = readResolvedColorValue(sliderThumbBorderToken, consumerSlider);
const sliderEditFocusBorderColor = readResolvedBorderToken('--uzu-edit-focus-border', consumerSlider);
const sliderSurfaceColor = readResolvedBorderToken('--uzu-surface', consumerSlider);
const sliderActionBgColor = readResolvedBorderToken('--uzu-action-bg', consumerSlider);
const sliderBoxShadow = sliderStyle.boxShadow;
focusVisible(consumerSlider);
await wait(0);
const sliderFocusedBoxShadow = getComputedStyle(consumerSlider).boxShadow;
consumerSlider.blur();
const consumerSteppedSlider = document.querySelector('#consumer-slider-stepped');
const steppedSliderValueBefore = consumerSteppedSlider.style.getPropertyValue('--uzu-slider-value').trim();
const steppedSliderStyle = getComputedStyle(consumerSteppedSlider);
const steppedSliderTrackHeightToken = steppedSliderStyle.getPropertyValue('--uzu-slider-track-height').trim();
const steppedSliderThumbSizeToken = steppedSliderStyle.getPropertyValue('--uzu-slider-thumb-size').trim();
const steppedSliderStepCount = steppedSliderStyle.getPropertyValue('--uzu-slider-step-count').trim();
const steppedSliderStepTicks = steppedSliderStyle.getPropertyValue('--uzu-slider-step-ticks').trim();
consumerSteppedSlider.value = '4';
consumerSteppedSlider.dispatchEvent(new Event('input', { bubbles: true }));
const steppedSliderValueAfter = consumerSteppedSlider.style.getPropertyValue('--uzu-slider-value').trim();
const steppedSliderStepTicksAfter = getComputedStyle(consumerSteppedSlider).getPropertyValue('--uzu-slider-step-ticks').trim();
const consumerUnevenSteppedSlider = document.querySelector('#consumer-slider-stepped-uneven');
const unevenSteppedSliderStyle = getComputedStyle(consumerUnevenSteppedSlider);
const unevenSteppedSliderValue = consumerUnevenSteppedSlider.style.getPropertyValue('--uzu-slider-value').trim();
const unevenSteppedSliderStepCount = unevenSteppedSliderStyle.getPropertyValue('--uzu-slider-step-count').trim();
const unevenSteppedSliderStepTicks = unevenSteppedSliderStyle.getPropertyValue('--uzu-slider-step-ticks').trim();
const plainInputFocusProbe = await readFocusProbe(consumerPlainInput);
const textareaFocusProbe = await readFocusProbe(consumerNotesTextarea);
const searchInputFocusProbe = await readFocusProbe(searchInput);
const passwordInputFocusProbe = await readFocusProbe(passwordInput);
const stepperInputFocusProbe = await readFocusProbe(stepperInput, stepper);
const comboboxInputFocusProbe = await readFocusProbe(comboboxInput);
comboboxInput.dispatchEvent(new KeyboardEvent('keydown', { key: 'Escape', bubbles: true, cancelable: true }));
await wait(0);
const stepperStyle = getComputedStyle(stepper);
const stepperInputStyle = getComputedStyle(stepperInput);
const stepperControls = stepper.querySelector('.uzu-stepper-controls');
const stepperControlsStyle = getComputedStyle(stepperControls);
const stepperButtonStyle = getComputedStyle(stepperIncrement);
const stepperShellBorderWidth = stepperStyle.borderTopWidth;
const stepperShellBorderRadius = stepperStyle.borderTopLeftRadius;
const stepperInputBorderWidth = stepperInputStyle.borderTopWidth;
const stepperInputBoxShadow = stepperInputStyle.boxShadow;
const stepperControlsDividerWidth = stepperControlsStyle.borderLeftWidth;
const stepperButtonBorderWidth = stepperButtonStyle.borderTopWidth;
const stepperButtonBackdropFilter = stepperButtonStyle.backdropFilter || stepperButtonStyle.webkitBackdropFilter || '';
const inputGroup = document.querySelector('#consumer-input-group');
const inputGroupInput = inputGroup.querySelector('.uzu-input');
const inputGroupAddon = inputGroup.querySelector('.uzu-input-addon');
const inputGroupAction = inputGroup.querySelector('.uzu-input-action');
const inputGroupCurrencySelect = inputGroup.querySelector('#consumer-input-group-currency');
const inputGroupCurrencyTrigger = inputGroupCurrencySelect.querySelector('[data-uzu-select-trigger]');
const inputGroupCurrencyMenu = inputGroupCurrencySelect.querySelector('.uzu-select-menu');
const inputGroupCurrencyEurOption = inputGroupCurrencySelect.querySelector('[data-value="eur"]');
const inputGroupStyle = getComputedStyle(inputGroup);
const inputGroupInputStyle = getComputedStyle(inputGroupInput);
const inputGroupAddonStyle = getComputedStyle(inputGroupAddon);
const inputGroupCurrencyTriggerStyle = getComputedStyle(inputGroupCurrencyTrigger);
const inputGroupDisplay = inputGroupStyle.display;
const inputGroupBorderWidth = inputGroupStyle.borderTopWidth;
const inputGroupBorderRadius = inputGroupStyle.borderTopLeftRadius;
const inputGroupBorderColor = inputGroupStyle.borderTopColor;
const inputGroupInputBorderWidth = inputGroupInputStyle.borderTopWidth;
const inputGroupAddonBackground = inputGroupAddonStyle.backgroundColor;
const inputGroupAddonBorderRightWidth = inputGroupAddonStyle.borderRightWidth;
const inputGroupCurrencySelectDividerWidth = getComputedStyle(inputGroupCurrencySelect).borderLeftWidth;
const inputGroupCurrencyTriggerBorderWidth = inputGroupCurrencyTriggerStyle.borderTopWidth;
const inputGroupCurrencyTriggerBackground = inputGroupCurrencyTriggerStyle.backgroundColor;
focusVisible(inputGroupCurrencyTrigger);
await wait(0);
const inputGroupCurrencyTriggerFocusedActive = document.activeElement === inputGroupCurrencyTrigger;
const inputGroupCurrencyTriggerFocusedBorderColor = getComputedStyle(inputGroup).borderTopColor;
const inputGroupCurrencyTriggerFocusedBoxShadow = getComputedStyle(inputGroup).boxShadow;
const inputGroupCurrencyTriggerFocusedTargetStyle = getComputedStyle(inputGroupCurrencyTrigger);
const inputGroupCurrencyTriggerFocusedTargetBoxShadow = inputGroupCurrencyTriggerFocusedTargetStyle.boxShadow;
const inputGroupCurrencyTriggerFocusedOutlineStyle = inputGroupCurrencyTriggerFocusedTargetStyle.outlineStyle;
const inputGroupCurrencyTriggerFocusedOutlineWidth = inputGroupCurrencyTriggerFocusedTargetStyle.outlineWidth;
const inputGroupCurrencyTriggerFocusedOutlineColor = inputGroupCurrencyTriggerFocusedTargetStyle.outlineColor;
const inputGroupCurrencyTriggerFocusedOutlineOffset = inputGroupCurrencyTriggerFocusedTargetStyle.outlineOffset;
inputGroupCurrencyTrigger.blur();
await wait(0);
focusVisible(inputGroupAction);
await wait(0);
const inputGroupActionFocusedActive = document.activeElement === inputGroupAction;
const inputGroupActionFocusedBorderColor = getComputedStyle(inputGroup).borderTopColor;
const inputGroupActionFocusedBoxShadow = getComputedStyle(inputGroup).boxShadow;
const inputGroupActionFocusedTargetStyle = getComputedStyle(inputGroupAction);
const inputGroupActionFocusedTargetBoxShadow = inputGroupActionFocusedTargetStyle.boxShadow;
const inputGroupActionFocusedOutlineStyle = inputGroupActionFocusedTargetStyle.outlineStyle;
const inputGroupActionFocusedOutlineWidth = inputGroupActionFocusedTargetStyle.outlineWidth;
const inputGroupActionFocusedOutlineColor = inputGroupActionFocusedTargetStyle.outlineColor;
const inputGroupActionFocusedOutlineOffset = inputGroupActionFocusedTargetStyle.outlineOffset;
inputGroupAction.blur();
await wait(0);
inputGroupInput.focus();
await wait(0);
const inputGroupFocusedBorderColor = getComputedStyle(inputGroup).borderTopColor;
const inputGroupFocusedBoxShadow = getComputedStyle(inputGroup).boxShadow;
const inputGroupFocusedInputBoxShadow = getComputedStyle(inputGroupInput).boxShadow;
inputGroupInput.blur();
click(inputGroupCurrencyTrigger);
await wait(60);
const inputGroupCurrencyOpenAnimation = getComputedStyle(inputGroupCurrencyMenu).animationName;
const inputGroupCurrencyMenuDisplay = getComputedStyle(inputGroupCurrencyMenu).display;
const inputGroupRect = inputGroup.getBoundingClientRect();
const inputGroupCurrencyMenuRect = inputGroupCurrencyMenu.getBoundingClientRect();
const inputGroupCurrencyMenuEscapesGroup = inputGroupCurrencyMenuRect.bottom > inputGroupRect.bottom + 2;
click(inputGroupCurrencyEurOption);
await wait(60);
const inputGroupCurrencyValue = inputGroupCurrencySelect.dataset.uzuSelectValue;
const inputGroupCurrencyLabel = inputGroupCurrencyTrigger.textContent.trim();
const inputGroupCurrencyExpandedAfterSelect = inputGroupCurrencyTrigger.getAttribute('aria-expanded');
const commandFocusProbe = await readFocusProbe(commandInput, command);
click(menuTrigger);
await wait(60);
const menuOpenAnimation = getComputedStyle(menuContent).animationName;
const menuHiddenDisclosureTargetHeight = readDisclosureTargetHeight('#consumer-menu-hidden-disclosure [data-uzu-disclosure-panel]');
const menuExpandedAfterOpen = menuTrigger.getAttribute('aria-expanded');
click(menuRename);
await wait(40);
const menuCloseAnimation = getComputedStyle(menuContent).animationName;
contextTarget.dispatchEvent(new MouseEvent('contextmenu', { bubbles: true, cancelable: true, view: window, clientX: 24, clientY: 24 }));
await wait(60);
const contextMenuOpen = contextMenu.classList.contains('is-open');
const contextMenuLeft = contextContent.getBoundingClientRect().left;
click(contextMenu.querySelector('[data-uzu-menu-value="open"]'));
click(menubar.querySelector('[data-uzu-menubar-value="edit"]'));
const menubarActiveValue = menubar.querySelector('.uzu-menubar-item.is-active')?.dataset.uzuMenubarValue || '';
commandInput.value = 'theme';
commandInput.dispatchEvent(new Event('input', { bubbles: true }));
await wait(40);
const commandVisibleCount = [...command.querySelectorAll('.uzu-command-item')].filter((item) => !item.hidden).length;
click(command.querySelector('[data-uzu-command-value="theme"]'));
const stepNavReview = stepNav.querySelector('[data-uzu-step-value="review"]');
click(stepNavReview);
const stepNavValue = stepNav.dataset.uzuStepNavValue;
const stepNavReviewCurrent = stepNavReview.getAttribute('aria-current');
const accordionSecond = accordion.querySelectorAll('[data-uzu-disclosure]')[1];
click(accordionSecond.querySelector('[data-uzu-disclosure-trigger]'));
await wait(260);
const accordionFirstOpen = accordion.querySelectorAll('[data-uzu-disclosure]')[0].classList.contains('is-open');
const accordionSecondOpen = accordionSecond.classList.contains('is-open');
hoverCard.querySelector('[data-uzu-hover-card-trigger]').focus();
await wait(80);
const hoverCardOpen = hoverCard.classList.contains('is-open');
const hoverCardHiddenDisclosureTargetHeight = readDisclosureTargetHeight('#consumer-hover-hidden-disclosure [data-uzu-disclosure-panel]');
const popoverInitiallyHidden = popoverContent.hidden;
const popoverInitialExpanded = popoverTrigger.getAttribute('aria-expanded');
click(popoverTrigger);
await wait(60);
const popoverOpen = popover.classList.contains('is-open');
const popoverExpandedAfterOpen = popoverTrigger.getAttribute('aria-expanded');
const popoverContentHiddenAfterOpen = popoverContent.hidden;
const popoverOpenAnimation = getComputedStyle(popoverContent).animationName;
const popoverHiddenDisclosureTargetHeight = readDisclosureTargetHeight('#consumer-popover-hidden-disclosure [data-uzu-disclosure-panel]');
document.dispatchEvent(new KeyboardEvent('keydown', { key: 'Escape', bubbles: true, cancelable: true }));
await wait(260);
const popoverClosedAfterEscape = !popover.classList.contains('is-open') && popoverContent.hidden;
click(popoverTrigger);
await wait(60);
click(document.body);
await wait(260);
const popoverClosedAfterOutsideClick = !popover.classList.contains('is-open') && popoverContent.hidden;
click(tagSelectable);
const tagSelectablePressed = tagSelectable.getAttribute('aria-pressed');
click(tagCloseable.querySelector('[data-uzu-tag-close]'));
const tagCloseableHidden = tagCloseable.hidden;
click(disclosureTrigger);
const disclosureOpenAnimation = getComputedStyle(disclosurePanel).animationName;
const disclosurePanelTargetHeight = Number.parseFloat(disclosurePanel.style.getPropertyValue('--uzu-disclosure-panel-height'));
click(disclosureTrigger);
const disclosureCloseAnimation = getComputedStyle(disclosurePanel).animationName;
const disclosureClosing = disclosure.classList.contains('is-closing');
const disclosureHiddenWhileClosing = disclosurePanel.hidden;`;
