export const browserExpressionMetricInteractions = `const dialogTrigger = document.querySelector('[data-uzu-dialog-target]');
const secondDialogTrigger = document.querySelector('[data-uzu-dialog-target="#consumer-dialog-two"]');
const dialog = document.querySelector('[data-uzu-dialog]');
const secondDialog = document.querySelector('#consumer-dialog-two');
const overlay = document.querySelector('[data-uzu-dialog-overlay]');
const dialogCloseEvents = [];
dialog.addEventListener('uzu-dialog-close', (event) => {
  if (event.detail.dialog !== dialog) return;
  dialogCloseEvents.push(event.detail.trigger === dialogTrigger ? 'first-trigger' : 'wrong-trigger');
});
click(dialogTrigger);
const dialogSiblings = [...overlay.parentElement.children].filter((child) => child !== overlay);
const dialogBackgroundIsolated = dialogSiblings.some((child) => child.hasAttribute('inert') && child.getAttribute('aria-hidden') === 'true');
const dialogOverlayInteractive = !overlay.hasAttribute('inert') && overlay.getAttribute('aria-hidden') !== 'true';
const dialogScrollLocked = document.documentElement.style.overflow === 'hidden' && document.body.style.overflow === 'hidden';
await wait(80);
const dialogHiddenDisclosureTargetHeight = readDisclosureTargetHeight('#consumer-dialog-hidden-disclosure [data-uzu-disclosure-panel]');
const callout = document.querySelector('.uzu-callout');
const style = getComputedStyle(callout);
const calloutTitle = getComputedStyle(callout.querySelector('.uzu-callout-title'));
const calloutBody = getComputedStyle(callout.querySelector('p'));
const calloutSignature = getComputedStyle(callout.querySelector('.uzu-signature'));
const alert = document.querySelector('#consumer-alert');
const alertStyle = getComputedStyle(alert);
const alertTitle = getComputedStyle(alert.querySelector('h3'));
const alertBody = getComputedStyle(alert.querySelector('p'));
const alertSuccess = getComputedStyle(document.querySelector('#consumer-alert-success'));
const alertWarning = getComputedStyle(document.querySelector('#consumer-alert-warning'));
const alertWidth = alert.getBoundingClientRect().width;
const alertSuccessWidth = document.querySelector('#consumer-alert-success').getBoundingClientRect().width;
const consumerButton = getComputedStyle(document.querySelector('#consumer-button'));
const consumerPrimary = getComputedStyle(document.querySelector('#consumer-primary'));
const consumerGhost = getComputedStyle(document.querySelector('#consumer-ghost'));
const consumerDanger = getComputedStyle(document.querySelector('#consumer-danger'));
const buttonTransform = consumerButton.transform;
const breadcrumb = getComputedStyle(document.querySelector('#consumer-breadcrumb'));
const toolbar = document.querySelector('#consumer-toolbar');
const toolbarStyle = getComputedStyle(toolbar);
const toolbarButton = document.querySelector('#consumer-toolbar-button');
const toolbarButtonStyle = getComputedStyle(toolbarButton);
const stat = getComputedStyle(document.querySelector('#consumer-stat'));
const statValue = getComputedStyle(document.querySelector('.uzu-stat-value'));
const editorMount = document.querySelector('#consumer-editor-mount');
const editorMountStyle = getComputedStyle(editorMount);
const editorMountFirstBlock = getComputedStyle(editorMount.querySelector('p'));
const editorMountCode = getComputedStyle(editorMount.querySelector('code'));
const codeStyle = getComputedStyle(document.querySelector('#consumer-code'));
const codeBlock = document.querySelector('#consumer-code-block');
const codeBlockStyle = getComputedStyle(codeBlock.querySelector('.uzu-code-block-body'));
const codeBlockCustomBackground = codeBlockStyle.backgroundColor;
const codeBlockCustomColor = codeBlockStyle.color;
const codeBlockCode = codeBlock.querySelector('code');
const codeBlockTokenCount = codeBlock.querySelectorAll('.uzu-code-token').length;
const codeBlockLanguage = codeBlockCode?.dataset.uzuCodeLanguage || '';
const codeHighlightApiAvailable = Boolean(window.Usuzumi?.highlightCode && window.Usuzumi?.highlightCodeBlocks);
const codeHighlightLanguages = window.Usuzumi.listCodeLanguages();
const codeHighlightHasTypeScript = window.Usuzumi.hasCodeLanguage('ts');
const codeHighlightFragment = window.Usuzumi.highlightCode('const answer = 1;', 'js').fragment;
const codeHighlightFragmentTokenCount = codeHighlightFragment.querySelectorAll('.uzu-code-token').length;
const codeCopyButton = codeBlock.querySelector('[data-uzu-code-copy]');
click(codeCopyButton);
await wait(80);
const codeBlockCopiedText = clipboardText;
const codeBlockCopyLabelExists = Boolean(codeCopyButton.querySelector('[data-uzu-code-copy-label]'));
const codeBlockCopySvgExists = Boolean(codeCopyButton.querySelector('svg'));
const codeBlockCopyDisplay = getComputedStyle(codeCopyButton).position;
const codeBlockPaddingTop = Number.parseFloat(codeBlockStyle.paddingTop);
const localizedCodeBlock = document.querySelector('#consumer-language-code-block');
const localizedCodeCopyButton = localizedCodeBlock.querySelector('[data-uzu-code-copy]');
const getActiveCodeCopyLabelText = () => Array.from(localizedCodeCopyButton.querySelectorAll('[data-uzu-code-copy-label]'))
  .find((label) => !label.hasAttribute('data-uzu-language-hidden'))
  ?.textContent.trim() || '';
window.Usuzumi.applyLanguage(document.documentElement, 'zh', '', 'zh-CN');
const localizedCodeCopyAriaZh = localizedCodeCopyButton.getAttribute('aria-label');
const localizedCodeCopyLabelZh = getActiveCodeCopyLabelText();
click(localizedCodeCopyButton);
await wait(80);
const localizedCodeCopiedZh = clipboardText;
window.Usuzumi.applyLanguage(document.documentElement, 'en', '', 'en');
const localizedCodeCopyAriaEn = localizedCodeCopyButton.getAttribute('aria-label');
const localizedCodeCopyLabelEn = getActiveCodeCopyLabelText();
click(localizedCodeCopyButton);
await wait(80);
const localizedCodeCopiedEn = clipboardText;
const noLabelCodeBlock = document.querySelector('#consumer-code-block-no-label');
const noLabelCodeCopyButton = noLabelCodeBlock.querySelector('[data-uzu-code-copy]');
const noLabelCodeCopySvgBefore = Boolean(noLabelCodeCopyButton.querySelector('svg'));
click(noLabelCodeCopyButton);
await wait(80);
const noLabelCodeCopiedText = clipboardText;
const noLabelCodeCopyTextDuringFeedback = noLabelCodeCopyButton.textContent.trim();
const noLabelCodeCopySvgDuringFeedback = Boolean(noLabelCodeCopyButton.querySelector('svg'));
await wait(1500);
const noLabelCodeCopySvgAfterRestore = Boolean(noLabelCodeCopyButton.querySelector('svg'));
const noLabelCodeCopyTextAfterRestore = noLabelCodeCopyButton.textContent.trim();
const uninitializedNoLabelBlock = document.createElement('div');
uninitializedNoLabelBlock.className = 'uzu-code-block';
uninitializedNoLabelBlock.innerHTML = '<pre class="uzu-code-block-body uzu-scroll"><code class="language-js">const lateCopy = true;</code></pre><button class="uzu-icon-button uzu-code-block-copy" type="button" data-uzu-code-copy aria-label="Copy code"><svg viewBox="0 0 24 24" aria-hidden="true" fill="none"><rect x="8" y="8" width="10" height="10" rx="1.8" stroke="currentColor" stroke-width="1.7"/></svg></button>';
document.body.append(uninitializedNoLabelBlock);
const uninitializedNoLabelButton = uninitializedNoLabelBlock.querySelector('[data-uzu-code-copy]');
window.Usuzumi.applyLanguage(document.documentElement, 'zh', '', 'zh-CN');
const uninitializedNoLabelHasSvgAfterLanguage = Boolean(uninitializedNoLabelButton.querySelector('svg'));
const uninitializedNoLabelTextAfterLanguage = uninitializedNoLabelButton.textContent.trim();
const uninitializedNoLabelAriaAfterLanguage = uninitializedNoLabelButton.getAttribute('aria-label');
window.Usuzumi.init(uninitializedNoLabelBlock);
click(uninitializedNoLabelButton);
await wait(80);
const uninitializedNoLabelCopiedText = clipboardText;
const uninitializedNoLabelTextDuringFeedback = uninitializedNoLabelButton.textContent.trim();`;
